# Code owners — auto-requested as reviewers on every PR that touches their
# patterns. Lines later in the file take precedence.
# See https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-security/customizing-your-repository/about-code-owners

# Default owners for everything. Both accounts so either can satisfy
# require_code_owner_reviews (a solo maintainer's workaround for "you
# can't approve your own PRs").
*           @roy-riotio @riot-io
